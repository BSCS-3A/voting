<?php
	// if(isset($_POST['vote-submit'])) {
	function submit(){
		echo "Lololol";
		// $positions = $conn->query("SELECT * FROM candidate_positions");
		// mysqli_data_seek($positions,0);
		// while ($poss = $positions->fetch_assoc()){
		// 	$choice =$_GET[$poss["hierarchy_id"]];
		// 	if($choice != "Abstain") {
		// 		$conn->query("UPDATE candidate SET total_votes = (SELECT total_votes FROM candidate WHERE candidate_id = $choice) + 1 WHERE candidate_id = $choice) ;

     	// 	}
   		// }
	}
	// }

	// else
	// {
	// 	echo "none";
	// }
?>
